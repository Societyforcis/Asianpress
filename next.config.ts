import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ['https://commotion-backed-vibes.ngrok-free.dev'],
  output: 'standalone',
};

export default nextConfig;